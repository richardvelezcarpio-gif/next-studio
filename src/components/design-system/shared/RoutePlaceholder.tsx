import { PageShell } from "../layouts/PageShell";
import { Section } from "../foundations/Section";
export function RoutePlaceholder({ locale, title }: { locale: "en" | "es"; title: string }) { const description = locale === "en" ? "This page is part of the Phase 1 route foundation." : "Esta página forma parte de la base de rutas de la Fase 1."; return <PageShell locale={locale}><Section><p style={{ color: "var(--color-blue)", fontWeight: 700 }}>NEXT STUDIO</p><h1 style={{ margin: "12px 0", fontSize: "clamp(2rem, 5vw, 3.5rem)" }}>{title}</h1><p style={{ maxWidth: 560, lineHeight: 1.6 }}>{description}</p></Section></PageShell>; }
