"use client";import Image from"next/image";import{useState}from"react";import type{SampleData}from"@/types/sample-generator";import s from"./SampleGenerator.module.css";export function SamplePreview({data}:{data:SampleData}){const[i,setI]=useState(0);const item=data.samples[i]??data.samples[0];return <article className={s.preview}><header><div>{data.logo?<Image src={data.logo} alt="Business logo" width={34} height={34} unoptimized/>:<b>{data.businessName.slice(0,1)||"B"}</b>}<strong>{data.businessName||"Your business"}</strong></div><em>{data.status}</em></header><div className={s.meta}><div><small>CLIENT</small><strong>{data.clientName||"Client name"}</strong><p>{data.company}<br/>{data.clientEmail}</p></div><div><small>SAMPLE</small><strong>#{data.number}</strong><p>Revision: {data.revision}<br/>{data.issueDate}</p></div></div><h2>{data.projectTitle||"Project title"}</h2>{data.samples.length>1&&<div className={s.tabs}>{data.samples.map((x,n)=><button key={x.id} onClick={()=>setI(n)}>{x.title||`Sample ${n+1}`}</button>)}</div>}{item&&(item.image?<Image className={s.image} src={item.image} alt={item.title} width={700} height={400} unoptimized/>:<p>{item.name}</p>)}{item&&<section className={s.block}><small>VERSION {item.version}</small><p>{item.description}<br/>{item.notes}</p></section>}<section className={s.block}><small>PROJECT DETAILS</small><p>{data.productType} · {data.size} · {data.material} · {data.color} · {data.finish}<br/>Quantity: {data.quantity} · Deadline: {data.deadline}<br/>{data.reviewInstructions}</p></section></article>}
