import type{OrderForm}from"@/types/order-form";const prefix="next-web-studio-order:";export function createOrderToken(){return`ord-${crypto.randomUUID().replace(/-/g,"").slice(0,8).toUpperCase()}`;}export function saveTemporaryOrder(token:string,data:OrderForm){localStorage.setItem(prefix+token,JSON.stringify(data));}export function getTemporaryOrder(token:string){const raw=localStorage.getItem(prefix+token);return raw?JSON.parse(raw) as OrderForm:null;}
