import type{SampleData}from"@/types/sample-generator";const prefix="next-web-studio-sample:";export function createSampleToken(){return`sam-${crypto.randomUUID().replace(/-/g,"").slice(0,8).toUpperCase()}`;}export function saveTemporarySample(token:string,data:SampleData){localStorage.setItem(prefix+token,JSON.stringify(data));}export function getTemporarySample(token:string){const raw=localStorage.getItem(prefix+token);return raw?JSON.parse(raw) as SampleData:null;}
