import type{InvoiceData}from"@/types/invoice";const prefix="next-web-studio-invoice:";export function createInvoiceToken(){return`inv-${crypto.randomUUID().replace(/-/g,"").slice(0,8).toUpperCase()}`;}export function saveTemporaryInvoice(token:string,invoice:InvoiceData){localStorage.setItem(`${prefix}${token}`,JSON.stringify(invoice));}export function getTemporaryInvoice(token:string){const raw=localStorage.getItem(`${prefix}${token}`);return raw?JSON.parse(raw) as InvoiceData:null;}
