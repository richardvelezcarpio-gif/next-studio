import type{PaymentRequest}from"@/types/payment-request";const prefix="next-web-studio-payment:";export function createPaymentToken(){return`pay-${crypto.randomUUID().replace(/-/g,"").slice(0,8).toUpperCase()}`;}export function saveTemporaryPayment(token:string,data:PaymentRequest){localStorage.setItem(prefix+token,JSON.stringify(data));}export function getTemporaryPayment(token:string){const raw=localStorage.getItem(prefix+token);return raw?JSON.parse(raw) as PaymentRequest:null;}
