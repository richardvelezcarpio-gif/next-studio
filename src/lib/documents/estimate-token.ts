import type{EstimateData}from"@/types/estimate";const prefix="next-web-studio-estimate:";export function createEstimateToken(){return`est-${crypto.randomUUID().replace(/-/g,"").slice(0,8).toUpperCase()}`;}export function saveTemporaryEstimate(token:string,data:EstimateData){localStorage.setItem(`${prefix}${token}`,JSON.stringify(data));}export function getTemporaryEstimate(token:string){const raw=localStorage.getItem(`${prefix}${token}`);return raw?JSON.parse(raw) as EstimateData:null;}
