import type{ProposalData}from"@/types/proposal";const prefix="next-web-studio-proposal:";export function createProposalToken(){return`pro-${crypto.randomUUID().replace(/-/g,"").slice(0,8).toUpperCase()}`;}export function saveTemporaryProposal(token:string,data:ProposalData){localStorage.setItem(`${prefix}${token}`,JSON.stringify(data));}export function getTemporaryProposal(token:string){const raw=localStorage.getItem(`${prefix}${token}`);return raw?JSON.parse(raw) as ProposalData:null;}
